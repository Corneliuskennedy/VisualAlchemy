'use client';

import { GradientColorPicker } from '@/components/ui/GradientColorPicker';

/**
 * Gradient Demo Page
 * 
 * Interactive tool to experiment with ocean gradient colors and parameters.
 * 
 * Access at: /gradient-demo
 * 
 * Use this to find the perfect ocean colors, then copy the code to use in your hero section.
 */
export default function GradientDemoPage() {
  return <GradientColorPicker />;
}

